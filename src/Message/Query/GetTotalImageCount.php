<?php

namespace App\Message\Query;

class GetTotalImageCount
{
    /*
        Inside the Message/ directory,
        create a new Query/ subdirectory.
        And inside of that, create a new PHP class called GetTotalImageCount.
        Even that name sounds like a query instead of a command:
        I want to get the total image count.
        And in this case, we can leave the query class blank:
        we won't need to pass any extra data to the handler.
     */

}