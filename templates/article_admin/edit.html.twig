{% extends 'content_base.html.twig' %}

{% block content_body %}
    <h1>Edit the Article! 🚀</h1>

    {#
        If you've never seen this before, we'll explain it in a minute.
        Now that we have a new imageFile field, let's go render it!
        Open edit.html.twig.
        Remove the HTML form - we're done with that.
    #}
    {#
        That's it! The simplest possible file upload setup: one field, one button.
    #}
    {{ include('article_admin/_form.html.twig', {
        button_text: 'Update!'
    }) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('js/admin_article_form.js') }}"></script>
{% endblock %}
